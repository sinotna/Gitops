apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: platform-apps
  namespace: default
spec:
  destination:
    namespace: platform-applications
    name: in-cluster
  project: platform
  source:
    path: applications
    repoURL: "https://github.com/sinotna/GitOps-Apps.git"
    targetRevision: main
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
